myApp {
  host = "localhost"
  host = ${?APP_HOST}
  port = 8080
  port = ${?APP_PORT}
  routes {
    # If ask takes more time than this to complete the request is failed
    askTimeout = 5s
  }
  shortenedUrlLength = 8
}